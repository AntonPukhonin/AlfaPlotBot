from datetime import datetime, date, timedelta
import datetime
from decimal import *

def get_chart(city_id, age_id):
	expenses = [25, 32, 34, 20, 25]
	categories = ['A', 'B', 'C', 'D', 'E']
	return categories, expenses
